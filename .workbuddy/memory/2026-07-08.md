# 2026-07-08 工作日志

## 多专家并行评审 github_news
- 用户要求"召唤多个专家打磨 App"。采用方式：在工作区用 4 个后台子代理并行扮演专家（架构/系统设计、UI/UX、性能与包体积、测试与稳定性），各自只读评审代码，最后由主代理汇总。
- 评审基线：flutter analyze 0 问题；flutter test 全过 0 失败。项目健康。
- 关键发现（已写入 docs/polish_plan.md）：
  - 高：presentation 违规依赖 data 类（github_token_card）；repo_detail→project 跨 feature 依赖；DataProvenance 仅 3 态且展示不全；ai_news 缺 seed 兜底；回退链路与缓存 codec 缺测试。
  - 中：3 个超 300 行文件；monitor 桌面双栏溢出；i18n 不一致多处；移动端趋势列表非惰性；repo_tile build 重算 trend；缺 RepaintBoundary；cached_network_image 死依赖；enforceCap 触发面窄；离线回退不一致；测试覆盖缺口。
  - 低：响应式/裸常量/构建参数等打磨项。
- 交付物：docs/polish_plan.md（统一打磨清单 + 实施顺序）。

## 可复用模式
- 多专家评审工作流：TaskCreate 建清单 → 并行 Agent(general-purpose, run_in_background) 各角色只读评审 → 主代理去重汇总成方案文件并 present_files。下次同类需求可直接复用。

## 架构合规改动（高优先级 1-2 已实现）
- 项1：rate limit DTO（GitHubRateLimitSnapshot/Bucket）从 profile/data 迁入 profile/domain，新建 `lib/features/profile/domain/github_rate_limit.dart`；github_token_card 由依赖 data 改为依赖 domain。修掉 presentation→data 违例（AGENTS.md）。
- 项2：projectSearchQueryProvider 从 project/application 迁出到新建 `lib/shared/providers/app_search_query_provider.dart`；repo_detail_sidebar 与 home/devintel_top_header 两处跨 feature 引用改为从 shared 引入（project 内部文件经 project_providers re-export 保持不变）。修掉跨 feature 依赖。
- 验证：flutter analyze 0 issues；flutter test 182 全过。改动后需注意 import 排序（directives_ordering，info 级）保持整洁。

## 全量打磨收尾 + 桌面端启动（用户"梭哈，全部都做，验证，最后给我打开桌面端"）
- 完成中/高优打磨：T9 DataProvenance 5 态重构（枚举+i18n+badge+11处 observed→live+4处默认 localFallback→seed）；T10 ai_news 种子兜底（新建 ai_news_seed_data.dart 7条；notifier 三态回退 live/freshCache/staleCache/seed；页头与首页预览 badge）；T11 单测（data_provenance 往返 + ai_news 种子/stale 回退）；T13 monitor 桌面双栏 LayoutBuilder 防溢出、profile 裸圆角、sidebar 背景一致性。
- 拦截生产级致命崩溃：repo_tile.dart 中 `_trendCache` 被声明为 `const Map`（不可变），putIfAbsent 抛 "Cannot modify unmodifiable map"，导致所有渲染 RepoTile 的界面真机崩溃。改为 `final Map` 修复（T12 性能批改动的遗留缺陷，被 golden + 全量 test 暴露）。
- 验证终态：flutter analyze = No issues found；flutter test = 187 全过（含修复后 golden 3/3）。
- 未做项：T14 超300行拆分（trending_desktop_view 326 / home_chart_helpers 307 / activity_page 302）与低优 17/20-23 评估为需独立重构轮次，未冒险执行；T13 残余 repo_detail not_found 空态、i18n 裸写一致性因风险未做。

## 构建/运行桌面端重要坑
- `flutter run -d windows` 偶报 MSB3027/MSB3021：WebView2Loader.dll 被已运行的旧 "GitHub情报站" 实例（github_news.exe）锁定，导致新构建无法复制 DLL。
- 禁止在 Git Bash 用 `taskkill //F //PID xxx`（// 被 MSYS 吞掉、静默失败）。必须用 PowerShell 工具：`Stop-Process -Id <pid> -Force` 或 `Stop-Process -Name github_news -Force`。杀掉旧实例后重跑即可。
- 桌面端启动成功：build\windows\x64\runner\Debug\github_news.exe，Dart VM Service 已就绪，窗口已呈现给用户。

## 商业级打磨第二轮（4 专家评审 + 实施，用户"继续用专家团，全部打磨，商业级"）
- 召集架构/UI-UX/性能/测试 4 专家并行只读评审，产出 docs/commercial_plan.md（P0-P3 带 file:line 清单）。
- P0 正确性：删 repo_detail not_found 误判（relatedRepos/contributors 空就显示"未找到"，误伤真实仓库，错误分支已覆盖 404）；修 activity_page 跨 feature import（→ core/domain/contributor_entity）；activity 假事件标注"示例数据"Chip（新增 i18n key project.activity.demo）；monitor 数据层 '刚刚'/'小时' 中文逻辑改为 pushedAt 日期判定（新增 _isToday）。
- P1 健壮性：新增 core/network/parallel.dart `gatherAll`（可选超时 + 单失败降级 + 全失败才抛 AppException），接入 monitor/tech_hotspot/project 三处 fetch-list；cache_meta_dao 增 pruneStale（main 启动调用，retain 2 天，最佳努力不抛）。
- P2 性能/UI：AI 资讯时间线每行 RepaintBoundary；header_search_field 双 rebuild 修复（onChanged!=null 时不本地 setState）；profile medium→_Desktop；main.dart 全局 ErrorWidget.builder 崩溃兜底（release 不显示红屏）。
- P1 测试：新增 local_database_test（schema 完整性 + clearAll + enforceCap no-op）；ai_news freshCache 口径断言。flutter analyze=0；flutter test=191 全过。
- T14：拆分 trending_desktop_view（329 → ~140 + trending_board_selector.dart + trending_list.dart，均 <300，feature 内无 data 类 import）。
- 可复用模式：`gatherAll` 适用于"逐项独立拉取、丢一两个不影响整体"的列表聚合（monitor/tech_hotspot/project）；不适用于 enrichment（trending/tech_hotspot 的 _withObservedHistory，丢项即丢数据）。
- 延期（下轮）：T14 其余 3 文件（home_chart_helpers 307 / activity_page 302 / local_database 311）；i18n 全量扫荡（~732 处裸中文）；IconButton 无障碍 tooltip；WebView 常驻（Stack overlay 避免错误态重建控件）；5 主屏 golden；release 构建加固(--obfuscate/版本)；头部去重(PageHeader 配置驱动)。

## 商业级打磨第三轮（WebView 常驻 + T14 收尾 + a11y + release 加固，用户"继续"/"全部继续"）
- WebView 常驻（Task #26）：webview_page.dart 错误态原先替换 InAppWebView 控件，导致错误↔内容切换时销毁重建 WebView2（丢滚动/进度、进程开销）。改为 InAppWebView 常驻 + Stack 叠加 ErrorView/进度条（`if (state._failed) ErrorView(...)`），控件不再被销毁。
- T14 剩余 3 文件拆分（Task #27），全部 <300 行、feature 内拆分、无 data 类 import：
  - activity_page.dart(314) → widgets/activity_events_card.dart（_EventSpec+ActivityEventsCard+_EventTile）+ widgets/activity_contributors_card.dart；主文件留 ActivityPage/_Body/_DigestView。
  - home_chart_helpers.dart(307) → home_chart_models.dart（HomeLegacyTab/HomeMetricSpec/HomeLegendItem+chart helpers+_windowedTrend）+ home_chart_widgets.dart（ChartWindowSegmented/HomeLegendDot）；删除原文件，repoint 4 消费者 + test/home_providers_test.dart。
  - local_database.dart(311) → 抽出 database_schema.dart（DDL+_kBootstrap+_kMigrations+bootstrapSchema/onUpgradeSchema 公开函数）；主文件留 open/enforceCap/clearAll（207 行）。注意：跨文件调用须把 _bootstrap/_onUpgrade 改为公开 bootstrapSchema/onUpgradeSchema（Dart 私有是 library 级）。
  - 坑：database_schema.dart import 必须是 `package:sqflite_common_ffi/sqflite_ffi.dart`（非 sqflite_common_ffi.dart）；test/ 目录也会 import 旧路径，拆分后须全局搜 test/。
- IconButton 无障碍 tooltip（Task #28）：
  - 17 个二级页 AppBar 返回按钮 `IconButton(icon: Icon(Icons.arrow_back))` → `BackButton(onPressed:)`，复用 Material 自带本地化 backButtonTooltip，无需逐文件引入 i18n（webview_page 已用此模式）。
  - 动作按钮硬编码 tooltip 改 i18n：repo_detail 收藏(收藏/取消收藏)、collect 取消收藏、monitor_alert_list_tile 与 monitor_recent_alerts 的已读/归档 Tooltip、monitor_page_header 新增监控、monitor_topics_page 取消监控、route_error_view 首页按钮。新增 a11y 键：back/bookmark_add/bookmark_remove/monitor_add/mark_read/mark_unread/archive/add_monitor/monitor_remove/home（zh_cn + en_us）。
- release 构建加固（Task #29）：Runner.rc 回退版本 1,0,0,0→0,1,0,1 对齐 pubspec 0.1.0+1（macro 路径已由 CMake FLUTTER_VERSION_* 注入）；runner.exe.manifest 已仅声明 Win10/11 supportedOS；RUN.md 新增 §2.1 文档化 `flutter build windows --release --obfuscate --split-debug-info=build/symbols` + 符号保管 + flutter symbolize + 版本注入链路 + Win10+ 要求。
- 终验（Task #30）：flutter analyze = No issues found；flutter test = 191 全过；flutter run -d windows 构建成功（19.4s）并启动，Dart VM Service 就绪，窗口已呈现。
- 可复用模式：①返回按钮 a11y 一律用 `BackButton` 而非裸 IconButton（免费本地化 tooltip）。②跨 library 拆分私有成员须改公开命名。③i18n 拆分后用 `find lib -name "*.dart" -exec wc -l` 复核无 >300 行残留。

## release 冒烟测试（补 #24）
- `flutter build windows --release` 成功（60.6s），产物 build\windows\x64\runner\Release\github_news.exe。
- EXE 版本资源验证：ProductVersion/FileVersion = 0.1.0+1（与 pubspec 一致，经 CMake FLUTTER_VERSION_* 注入 Runner.rc），ProductName/FileDescription = GitHub情报站。版本跟踪链路确认通畅。
- #24 发布加固完成（obfuscate 文档 + 版本跟踪 + Win10+ manifest + release 冒烟）。唯一延期：头部去重（PageHeader 配置驱动，Task #31，可选重构非阻塞）。
- 终态：30 项任务 29 完成，仅 #31 头部去重延期。

## 商业级打磨第四轮（用户"继续全部打磨"：i18n 扫荡 + 5 主屏 golden + 头部去重）
- i18n 扫荡（Task #32）：先精准统计——lib/ 中 627+ 裸中文绝大多数在字典本身/seed-demo/domain/core-infra/文档注释（这些不 i18n）。真正用户可见硬编码集中在 monitor 特性与首页 demo 指标卡。
  - monitor 特性全量 i18n：monitor_alerts_page、monitor_settings_page、monitor_monitored_repos、monitor_settings_cards、monitor_rules_page、local_content_controller、profile_rules_page。新增 monitor.* 键 ~35 条（zh_cn+en_us）：告警列表/空态/筛选/全部已读/清空已读/恢复归档、通知设置/渠道/免打扰、监控仓库标题/空态/状态正常、通知项 7 条、规则标题/计数/空态/启用停用、4 条规则描述。
  - 关键重构：`monitorNotificationLabels` 与 `monitorRuleLabels` 由 `const List<String>` 改为 `List<String> f(AppLocalizations l10n)` 函数；新增 `monitorNotificationCount`/`monitorRuleCount` 常量供校验（原 `.length` 用法改常量）。调用点改为 `monitorXxxLabels(l10n)[i]`，并在各 build 加 `final l10n = AppLocalizations.of(context)`。
  - 有意排除：首页 demo 指标卡（home_tablet_metrics_row / home_today_stack）是占位假数据（硬编码 128/42.8K 等），i18n 化 32 条假标签价值极低且固化临时键；真实接入 API 后由带 i18n 的数据驱动。trending/ai_news/profile/tech_hotspot 的 presentation 页此前已 i18n 化（剩余中文均在注释）。
  - 坑：①改函数签名后，原先 `const Text('x')`/`const EmptyView(message: l10n.tr())`/`segments: const [` 因含 l10n 调用需去掉 const；②`const_eval_method_invocation` 报在 Text(l10n.tr()) 内层，去掉外层 const[ ] 即解；③`prefer_const_constructors` 在 `EdgeInsets.fromLTRB` 可加回 const。
- 5 主屏 golden（Task #33）：沿用现有自研 pumpScene + matchesGoldenFile（无 golden_toolkit 依赖）。新增 test/shared/widgets/main_screens_golden_test.dart，渲染 PageHeader 在 5 种主屏配置（home/ai_news/trending/tech_hotspot/monitor，真实 i18n 键 + 搜索框 + HeaderStatPill + 刷新/动作按钮），`--update-goldens` 生成 5 个基线 PNG 于 test/shared/widgets/goldens/main_header_*.png。测试数 191→196。
- 头部去重（Task #34）：在 page_header.dart 新增 `HeaderAction`（统一 constraints: minWidth:44/minHeight:44 + padding:zero + 默认 iconSize:20 + tooltip/onPressed），替换 6 个 feature header 的重复 IconButton 样板（trending/monitor/ai_news/profile/project/tech_hotspot）。devintel_top_header 的铃铛含通知红点(自定义 Stack)保留为特例。
- 终验：flutter analyze = No issues found；flutter test = 196 全过（+5 golden）；flutter run -d windows 构建成功并启动，Dart VM Service 就绪。
- 可复用模式：①i18n 扫荡先用 `grep -P "[\x{4e00}-\x{9fff}]"` 过滤，再用 `grep -P "Text\(|tooltip:|message:|label:|title:|subtitle:"` 只抓 UI 位置字符串，排除注释/seed/data。②const 列表含 l10n 时改为接收 l10n 的函数 + 独立 count 常量。③golden 测试复用现有 pumpScene 模式最稳，不必引 golden_toolkit。
- 当前轮次收尾：原 30 任务全完成；本轮 #32/#33/#34 完成。项目达到：analyze 0 问题、196 测试全过、5 主屏 golden 基线、全特性 UI 文案 i18n 化（demo 占位除外）、头部样板收敛、release 加固 + 冒烟通过。


